export enum WWMessageType {
    CANCEL = 1,
    RESULT = CANCEL << 1,
    VIEWPORT_TILES = RESULT << 1,
    VIEWPORT_CLUSTER = VIEWPORT_TILES << 1,
    CONFIGURE_PORTS = VIEWPORT_CLUSTER << 1,
    CACHE_OR_REQUEST = CONFIGURE_PORTS << 1,
    INVALIDATE_CACHE = CACHE_OR_REQUEST << 1,
    DONE = INVALIDATE_CACHE << 1,
    REMOVE_PORTS = DONE << 1,
    CALCULATE_TILES = REMOVE_PORTS << 1,
    HANDLING_TILES = CALCULATE_TILES << 1,
    FILTER_SHAPES = HANDLING_TILES << 1,
    ERROR = FILTER_SHAPES << 1,
    DELETEOVERLAY = ERROR << 1,
    TERMINATE_WORKER = DELETEOVERLAY << 1,
    CANCEL_REQUESTS = TERMINATE_WORKER << 1,
    DO_CLUSTER = CANCEL_REQUESTS << 1,
    FIND_CLOSEST_CLUSTER = DO_CLUSTER << 1,
    CALC_CLUSTER_STATS = FIND_CLOSEST_CLUSTER << 1,
    CONFIG_WORKER = CALC_CLUSTER_STATS << 1,
    VORONOI_BUILDER = CONFIG_WORKER << 1,
    CLIPPING_VORONOI_CLUSTER = VORONOI_BUILDER << 1
}
