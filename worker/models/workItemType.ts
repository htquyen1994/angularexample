export enum WorkItemType {
    UPDATEOVERLAY,
    DELETEOVERLAY,
    CANCELOVERLAY,
    INVALIDCACHE,
    CONFIG_WORKER
}

export enum ErrorType {
  WORKER_INIT,
  COMMON,
  UNAUTHORIZED,
  SERVER_BREAKDOWN
}

